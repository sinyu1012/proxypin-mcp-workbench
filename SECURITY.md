# 安全说明

## 适用边界

ProxyPin MCP Workbench 是面向开发与授权测试的 HTTPS 调试工具。请只分析自己拥有或明确获授权的设备、账号、应用和流量。

禁止使用本项目绕过第三方付费权益、访问控制或证书固定，也不要在未经授权的设备上安装 CA。

## 敏感数据

请勿向公开仓库、Issue 或日志提交以下信息：

- Authorization、Cookie、Set-Cookie、访问令牌和刷新令牌
- 带凭据的查询参数或完整请求 URL
- HAR、抓包历史、运行时配置和导出数据
- 设备标识、精确位置、账号资料、儿童或家庭成员数据
- CA 私钥、PKCS#12 文件或仍在使用的客户端证书

仓库内的 MCP 辅助脚本默认对常见凭据字段做脱敏，但自动脱敏不能替代发布前人工检查。

## CA 风险

安装并信任抓包 CA 后，持有对应私钥的程序可以解密经过其代理的 HTTPS 流量。仅安装由当前可信构建在本机生成、且名称或指纹可以核对的 CA。

本分支已经移除安装包中的共享 CA 私钥。新安装会在首次启动时于本机应用数据目录生成唯一 CA 与私钥；macOS/Linux 上的私钥文件会限制为仅当前用户可读写。由旧版升级时，原有本机证书不会被静默替换，请主动在证书管理中重新生成 CA，并重新安装和核对证书。

本公开仓库从脱敏快照开始记录历史，不包含上游历史中的旧测试私钥。上游作者、许可证和原始仓库链接保留在 README 与 NOTICE 中；需要追溯原始实现时，请查阅相应上游仓库。

抓包结束后建议：

1. 关闭手机 Wi-Fi 代理。
2. 停止 HTTPS 解密。
3. 删除不再需要的描述文件和受信任根证书。
4. 清理包含请求凭据的截图、HAR、日志和临时导出文件。

## 报告漏洞

请通过 GitHub Security Advisory 私下报告可能导致凭据泄露、远程访问、任意代码执行或证书私钥泄露的问题。报告中不要附带真实用户数据或仍有效的凭据。
